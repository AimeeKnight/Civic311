extends ../shared/template

block content
  .row
    .small-12.columns
      form#forms(method='post', action='/admin/login')
        fieldset
          legend=title
          label Email
          input(type='email', name='email', autofocus=true)
          label Password
          input(type='password', name='password')
          button.radius.small Login

